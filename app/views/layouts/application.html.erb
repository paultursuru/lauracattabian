<!DOCTYPE html>
<html>
  <head>
    <title>Lauracatta</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <div class="d-flex">
      <div class="col-md-4 col-lg-3">
        <%= render 'layouts/left_bar' %>
      </div>
      <div class="col-md-6 col-lg-7 d-flex flex-wrap main-content">
        <%= yield %>
      </div>
      <% if user_signed_in? %>
        <div class="col text-center mt-5 options">
          <div class="btn btn-danger darklogout">
            <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
          </div>
          <div class="btn btn-primary darklogout">
            <%= link_to 'Tutti', articles_path %>
          </div>
          <div class="btn btn-success darklogout">
            <%= link_to 'Nuovo', new_article_path %>
          </div>
          <!-- add more buttons here -->
        </div>
      <% end %>
    </div>
    <%#= render 'layouts/flashes' %>
  </body>
</html>
